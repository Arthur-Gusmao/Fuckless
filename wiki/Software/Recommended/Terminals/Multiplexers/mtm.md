## mtm

A micro terminal multiplexer.

mtm is perhaps the smallest useful terminal multiplexer in the world.

### Less is more

Instead of competing with tmux by adding features, mtm removes everything that is not essential.

### One job

mtm follows the Unix philosophy better than most modern terminal multiplexers.

It focuses exclusively on splitting a terminal into multiple virtual terminals. Nothing more.
